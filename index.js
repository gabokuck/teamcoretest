/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import {TeamcoreApp} from './src/TeamcoreApp';

AppRegistry.registerComponent(appName, () => TeamcoreApp);
